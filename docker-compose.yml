services:
  hookwatch:
    image: hookwatch:local
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      HOOKWATCH_PORT: "8080"
      HOOKWATCH_DATA_DIR: /data
    volumes:
      - hookwatch-data:/data
    restart: unless-stopped

volumes:
  hookwatch-data:
